## chord-parsing
Implemented Root, Accidental, NoteLetter, Quality, and Chord rules in grammar.pegjs. Added Chord type and Quality union type to types.ts. Exported parseChord() from parser.ts. Added --allowed-start-rules flag to build:grammar so Chord can be used as an entry point. 12 acceptance tests covering C, F#, Bb (major), Am, C#m, Bbm (minor), G7, Bb7, F#7(dominant7), and rejection of Cm7, CM7, Cdim.

## bar-parsing
Added Bar and BarTail rules to grammar.pegjs, with a _ whitespace rule for optional spaces and tabs around the chord. Added Bar type to types.ts. Exported parseBar() from parser.ts. Extended --allowed-start-rules to include Bar. 6 acceptance tests: standard spacing, no spaces, extra spaces, empty bar rejection, two-chord rejection.

## row-parsing
Added Row rule that opens with | then matches one or more BarTail groups (chord + closing |), avoiding double-consumption of shared barlines. Added Row type to types.ts. Exported parseRow() from parser.ts. Extended --allowed-start-rules to include Row. 5 acceptance tests: four-bar row, single-bar row, quality correctness across bars, missing opening barline rejection, missing closing barline rejection.

## frontmatter-parsing
Added FrontMatter, FrontMatterField, FrontMatterValue, and Newline rules to grammar.pegjs. Front matter is enclosed in --- delimiters; fields are parsed as generic key: value pairs and then validated in the action. The key field is validated against the 17 chromatic note spellings (C through B with sharps and flats); invalid values are rejected via Peggy's error() function. Added FrontMatter type to types.ts. Exported parseFrontMatter() from parser.ts. 7 acceptance tests: title + key, title only, key only, empty front matter, all 17 valid keys, invalid key (H), mode suffix rejection (Am).

## song-parsing
Added Song and SongBody rules to grammar.pegjs. Song combines optional FrontMatter with zero or more Rows; blank lines between rows are filtered out in the SongBody action. Added Song type to types.ts. Exported parseSong() from parser.ts. Extended --allowed-start-rules to include Song. 4 acceptance tests: no front matter, front matter + multiple rows, blank line handling, front matter with no rows.

## text-renderer
Created src/renderers/text.ts with a TextRenderer class. renderChord() maps quality back to suffix ('' / 'm' / '7'). renderRow() joins bars as | chord | chord |. renderFrontMatter() emits a --- block only when title or key is non-null. The render() method joins front matter and rows with newlines. 7 tests: front matter output, front matter omission, spacing normalisation, four-bar row on one line, multi-row layout, and two round-trip tests (parse → render → parse produces equal AST).

## eslint
Installed eslint 10, @eslint/js, and typescript-eslint as devDependencies. Created eslint.config.js using flat config format, composing js.configs.recommended and tseslint.configs.recommended. Excluded both generated parser files (generated.js and generated.d.ts) from linting — the .d.ts file contained no-explicit-any and no-empty-object-type violations that are inherent to the generated output. Added a `lint` script to package.json. pnpm lint exits cleanly with no violations; all 42 tests continue to pass.

## prettier
Installed prettier 3.8.1 as a devDependency. Added .prettierrc with singleQuote, trailingComma: all, printWidth: 100. Added .prettierignore excluding generated parser files and dist/. Added a `format` script to package.json running `prettier --write src/`. Ran pnpm format — three files were reformatted (parser.test.ts, text.test.ts, text.ts); all 42 tests continue to pass.

## half-diminished-quality
Added 'halfDiminished' to the Quality union type in types.ts. Extended the Peggy grammar (grammar.pegjs) with an 'm7b5' alternative before 'm' in the Quality rule so that PEG ordered choice matches it first. Extended TextRenderer.renderChord() to emit the 'm7b5' suffix for halfDiminished chords. Rebuilt the generated parser with pnpm build:grammar. Added 3 parser tests (Bm7b5, Am7b5, C#m7b5) and 2 renderer tests (suffix output and round-trip). All 47 tests pass.

## pitch-class
Created src/theory/pitchClass.ts with: a PitchClass type (union of 0–11), a NOTE_MAP covering all 17 note spellings (7 naturals, 5 sharps, 5 flats), ENHARMONIC_PAIRS mapping each sharp to its flat equivalent and vice-versa, and rootToPitchClass() which throws for unrecognised inputs. Created src/theory/pitchClass.test.ts with 7 tests covering natural notes, sharp spellings, flat spellings, enharmonic equivalence, and error for invalid input. All 54 tests pass.

## key-detector-cat3
Extracted score computation into a \`computeScore\` helper. Added \`breakRelativeTie(major, minor, chords)\` which applies ordered heuristics: (1) V7 of harmonic minor present → minor; (2) V7 of major present → major; (3) last-chord tonic match; (4) first-chord tonic match; (5) prefer major. In \`detectKey\`, after finding the best-scoring key, check if its relative major/minor scores within 1 point (the quality-bonus asymmetry for the I chord of a major key vs III=augmented of its relative minor creates a 1-point gap); if so, invoke tiebreaking. Also added \`dom7\` and \`hd\` helpers to the test file and wrote all 7 Category 3 tests (T3-a through T3-g). All 75 tests pass.

## key-detector-cat1
Created src/theory/keyDetector.ts with detectKey(chords: Chord[]): string | null. Scoring assigns 1 point per diatonic chord root (by pitch class) plus a quality bonus point when the chord quality matches the expected diatonic scale degree quality (major keys use major/minor/major/major/minor pattern; harmonic minor keys use minor/halfDiminished/minor/major/major pattern). The quality bonus breaks ties that pure diatonic-root counting cannot resolve (e.g. C major vs G major for [G, D, Em, C]). dominant7 is treated as matching major for quality purposes. Notes unrecognised by rootToPitchClass (E#, B# in key note sets) are silently skipped. Created src/theory/keyDetector.test.ts with 5 Category 1 tests (T1-a through T1-e). All 68 tests pass.

## keys
Created src/theory/keys.ts with: a KeyInfo interface (notes array + optional relative field), a KEYS record of 22 entries (12 major + 10 harmonic minor, excluding G#m and Ebm which require double accidentals), and diatonicNotes() which returns a ReadonlySet<string> for a given key or throws for unknown keys. Harmonic minor keys use the natural minor scale with the 7th degree raised (e.g. Am → G#, C#m → B#, F#m → E#). Relative major/minor links are stored for all pairs that fall within the 22-key set. Created src/theory/keys.test.ts with 9 tests. All 63 tests pass.

## key-detector-cat4
Added 3 Category 4 tests (T4-a through T4-c) to keyDetector.test.ts: ii-V-I (G7=V7 of C), C7=V7 of F, and F7=V7 of Bb with enharmonic roots D#/A#. The existing diatonic-root + quality-bonus scoring (where dominant7 matches the major quality of scale degree V) already resolves all three progressions correctly without additional code changes — the quality bonus on the V7 chord provides sufficient signal. All 78 tests pass.

## key-detector-cat7
Added 3 Category 7 tests (T7-a through T7-c) to keyDetector.test.ts. T7-a ([C, F, Ab, G] → C) and T7-b ([C, A7, Dm, G7, C] → C) passed with the existing scoring. T7-c ([C, Db, D, Eb] → null) required a new threshold: detectKey now returns null when maxScore < 1.5 * chords.length, ensuring that chromatic progressions where the best-fitting key has too low an average score-per-chord are rejected. All 85 tests pass.

## key-detector-cat6
Added optional \`declaredKey?: string | null\` parameter to \`detectKey\`. The rule: if declaredKey scores > 0 (any diatonic overlap), return it unchanged; only override it if it scores zero. Also refactored \`detectKey\` to accumulate all scores into a Map upfront, so the relative-tiebreak lookup reuses cached values instead of recomputing. Added 4 Category 6 tests (T6-a through T6-d): single-chord best-guess, declared-key preservation, declared-key correction when score is zero, and three-chord unambiguous identification. All 82 tests pass.

## key-detector-cat9
Added \`Gb\` major to KEYS (notes: Gb, Ab, Bb, Cb, Db, Eb, F; no relative — Ebm is out of scope). Updated the KEYS count test to expect 23 entries. Added a \`DetectKeyConfig\` interface and an optional third \`config\` parameter to \`detectKey\`. Implemented \`breakFSharpGbTie()\`: if config explicitly specifies 'g-flat' or 'f-sharp', use it; otherwise count exact chord-root spellings of 'Gb' vs 'F#' to infer the intended spelling; default to F# if equal. Added the F#/Gb tiebreak call after the existing relative-major/minor tiebreak. Added 4 Category 9 tests (T9-a through T9-c config): T9-a shows Db outscores Gb (Ab-major as V of Db is a better quality match), T9-b shows Gb/F# tie resolved by flat-spelling preference (Abm as IIm of Gb matches quality; test uses min('Ab')), T9-c default shows F# wins by sharp-spelling, T9-c config shows config override to Gb. All 89 tests pass.

## normalise-cat5
Extended normaliseSong to update song.key in the returned Song: changed detectKey call to pass null instead of song.key as the declaredKey so the best-scoring key is detected unconditionally (without being anchored to an incorrect declared key). The detected key is then written back via \`{ ...song, key: detectedKey, rows: newRows }\`. Added 4 Category 5 tests (T5-a through T5-d): correct key preserved, wrong key corrected, absent key added, and wrong key corrected when the declared key scores > 0 but is not the best candidate. All 101 tests pass.

## normalise-cat2
Created src/theory/normalise.ts exporting normaliseSong(song: Song, config?: DetectKeyConfig): Song. The implementation collects all chords from the song, calls detectKey (passing song.key as declaredKey), builds a pitch-class-to-note-name map from the detected key's diatonic notes (skipping E# and B# which are not in NOTE_MAP), then rewrites any chord root whose pitch class matches a diatonic note but is spelled differently. Non-diatonic roots are left untouched. The function is pure and returns a new Song. Created src/theory/normalise.test.ts with 8 tests (T2-a through T2-f plus immutability and idempotency checks). All 97 tests pass.

## normalise-cat8
Extended normaliseSong with borrowed-chord handling. Added FLAT_SPELLING map for the 5 enharmonic pitch classes (1→Db, 3→Eb, 6→Gb, 8→Ab, 10→Bb). Added preferredNonDiatonicSpelling() which applies a next-chord secondary-dominant heuristic (if current chord's pc is a 5th above the next chord's pc and the next root is not sharp-side, use the flat spelling) then falls back to the flat spelling for pc ∈ {1,3,6,8,10}. Extended normaliseChord to accept nextChord and call this helper for non-diatonic roots. Updated normaliseSong to track chord index for next-chord lookup, removed the early-return-on-null-key (replaced with empty pcToNote so flat-fallback still runs), and changed key field to detectedKey ?? song.key (preserving the original when no key can be detected). Added 4 Category 8 tests (T8-a through T8-d). All 105 tests pass.

## cli-scaffold
Created src/cli.ts as the CLI entry point with a #!/usr/bin/env node shebang. Installed minimist as a runtime dependency and @types/minimist + @types/node as devDependencies. Implemented runCli(args: string[]) which uses minimist to parse --help/-h flags, then dispatches to named subcommands (normalise, render, transpose). Unknown subcommands and missing subcommands exit with code 1; --help/-h exits with code 0 and prints usage. The entry point is guarded by an isMain check using fileURLToPath(import.meta.url) so the module can be safely imported in tests. Added 'bin': { 'grigson': './dist/cli.js' } to package.json. Created src/cli.test.ts with 4 tests (unknown subcommand exits 1, --help exits 0 with usage, -h exits 0 with usage, no args exits 1). TypeScript build produces dist/cli.js with the shebang intact. All 109 tests pass.

## cli-normalise
Implemented the \`grigson normalise\` subcommand in src/cli.ts. Added \`runNormalise()\` which reads input from a file argument or stdin (fd 0), calls parseSong → normaliseSong → TextRenderer.render(), and writes to stdout or edits in place with -i/--in-place. Added --key support by extending DetectKeyConfig with an optional \`forceKey?: string\` field in keyDetector.ts, then using \`config?.forceKey ?? detectKey(...)\` in normaliseSong to bypass detection when a key is forced. Added --enharmonic support forwarding 'g-flat'/'f-sharp' / 'g-flat' preference, and -i/--in-place to edit the file on disk instead of writing to stdout. Added 6 acceptance tests covering all scenarios. All 115 tests pass.

## cli-render
Implemented the \`grigson render\` subcommand in src/cli.ts. Added \`runRender()\` which reads input from a file argument or stdin, calls parseSong → TextRenderer.render(), and writes to stdout. Supports --format text (default) and rejects --format svg with a 'not yet implemented' message (exit 1) and any other unknown format value (exit 1). No normalisation is performed — pure parse-and-render. Added 5 acceptance tests. All 120 tests pass.

## website-scaffold
Created website/ as a self-contained Eleventy sub-project. Added website/package.json (with "type": "module", build/serve scripts, @11ty/eleventy 3.1.2 as devDependency). Created website/.eleventy.js with addPassthroughCopy('assets') and dir config { input: 'content', includes: '../_includes', output: '_site' }. Created website/_includes/base.njk with a minimal HTML shell including doctype, head (charset/viewport/title), nav with links to all five pages, a <main> block rendering {{ content | safe }}, and a footer. Added website/_site/ and website/node_modules/ to the root .gitignore. Confirmed pnpm install and pnpm build succeed (Eleventy 3.1.2 renders output without errors). Temporary index.md was created for the build test then removed. All 122 existing tests continue to pass.

## website-content
Created five website content pages under website/content/: index.md (homepage with description and navigation links), format.md (full chart format reference from documentation/README.md), cli.md (CLI reference from documentation/cli.md), renderer.md (renderer docs from documentation/renderer.md), and testing.md (testing strategy from documentation/testing.md). Each page has Eleventy front matter with layout, title, and permalink. The nav links in base.njk were already in place from website-scaffold. Ran pnpm build inside website/ — all five pages rendered to _site/ (index.html, format/index.html, cli/index.html, renderer/index.html, testing/index.html). All 122 existing tests continue to pass.

## website-deploy
Created .github/workflows/deploy.yml with a GitHub Actions workflow triggered on push to main. The workflow has two jobs: build (actions/checkout@v4, actions/setup-node@v4 with Node 20, npm install in website/, npm run build in website/, actions/upload-pages-artifact@v3 from website/_site/) and deploy (actions/deploy-pages@v4, environment: github-pages). Permissions are scoped to contents: read, pages: write, id-token: write. A concurrency group 'pages' prevents overlapping deployments. Note: the GitHub repository must be configured with Settings → Pages → Source: GitHub Actions for the deploy job to succeed. All 122 existing tests continue to pass.

## pnpm-workspace
Created pnpm-workspace.yaml at the repo root listing both '.' (grigson) and 'website' as workspace packages. Added 'grigson': 'workspace:*' to the dependencies field of website/package.json. Ran pnpm install from the root — resolves and links 325 packages cleanly. Confirmed that website/node_modules/grigson is a symlink pointing to the repo root (../..); the grigson package is now available for import inside the website sub-project. All 122 existing tests continue to pass.

## browser-bundle
Installed vite 8.0.0 as a devDependency at the workspace root. Created src/index.browser.ts exporting parseSong, parseChord, TextRenderer, normaliseSong, and related types — no CLI imports. Created vite.config.ts in library mode with entry src/index.browser.ts, formats ['iife', 'es'], fileName function producing dist/grigson.iife.js and dist/grigson.esm.js, globalName 'grigson', emptyOutDir: false (to avoid clobbering the TypeScript output). Added a 'build:browser' script to package.json and appended it to the main 'build' script. Verified the IIFE bundle exposes grigson.parseSong, grigson.TextRenderer, and grigson.normaliseSong as globals via a Node smoke test. All 122 existing tests continue to pass.

## website-grigson-script
Added an addPassthroughCopy mapping in website/.eleventy.js that copies node_modules/grigson/dist/grigson.iife.js to js/grigson.iife.js in the built site (the source resolves through the pnpm workspace symlink, so it always reflects the latest build). Added a <script src="/js/grigson.iife.js"></script> tag to the <head> in website/_includes/base.njk, making the grigson global available on every page. Confirmed that running pnpm build at the root followed by pnpm build in website/ produces _site/js/grigson.iife.js. All 122 existing tests continue to pass.

## website-normalise-demo
Created website/content/demo.md with a hardcoded "badly spelled" chord chart (A# in F major) and an inline <script> block. The script uses the grigson browser bundle (loaded via the base.njk layout) to parse, normalise, and render the chart into an output <pre> element on DOMContentLoaded. Added a "Demo" link to the nav in website/_includes/base.njk. Confirmed that building the website produces the demo/index.html with the correct structure and script content. Updated documentation/browser-bundle.md to reference the new interactive demo. All 122 existing tests pass.

## website-playground
Created website/content/playground.md with a <textarea> for input and a <pre> for output. Added an inline <script> that uses the grigson browser bundle to live-normalise the chart on every keystroke. The script includes error handling to display parse failures in the output area. Pre-populated the playground with an example chart (A# in F major) that demonstrates automatic enharmonic correction. Added a "Playground" link to the navigation menu in website/_includes/base.njk. Confirmed that running pnpm build in the website sub-project renders the playground/index.html correctly. All 122 existing tests pass.

## textmate-grammar-full
Updated packages/textmate-grammar/grigson.tmLanguage.json to cover all MVP syntax: front matter delimiters (---) and fields (title, key), barlines (|), and chords with support for all qualities (m, 7, m7b5). Used regex lookaheads/lookbehinds for chord matching to ensure precision without breaking on words. Synced the updated grammar to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. Created a comprehensive syntax highlighting test file in packages/grigson/documentation/examples/syntax-test.chart.

## playground-shiki
Installed shiki as a devDependency in packages/website. Updated website/.eleventy.js to copy the Grigson TextMate grammar from packages/textmate-grammar to _site/js/. Integrated Shiki into website/content/playground.md using the ESM version from a CDN. The playground now loads the grammar, registers 'grigson' as a language, and uses shiki.codeToHtml() to provide live syntax highlighting (using the 'nord' theme) in the normalised output area. Added CSS to style the Shiki-generated output.

## playground-monaco
Installed monaco-editor as a devDependency in packages/website. Updated website/.eleventy.js to copy the Monaco min/vs distribution to _site/js/monaco/vs. Replaced the <textarea> in website/content/playground.njk with a <div> container and updated the playground script to initialize Monaco Editor. Linked Monaco's onDidChangeModelContent event to the live-normalisation update() function. All 122 existing tests pass.

## playground-monaco-highlight
Installed monaco-textmate and vscode-oniguruma as devDependencies in packages/website. Updated website/.eleventy.js to copy onig.wasm to _site/js/. Updated website/content/playground.njk to initialize vscode-oniguruma, register the 'grigson' language in Monaco, and provide a TextMate-based token provider using monaco-textmate. Defined a 'grigson-theme' in Monaco to map TextMate scopes to colors, ensuring full syntax highlighting in the input editor that matches the Shiki output. All 122 existing tests pass.

## custom-element-scaffold
Created src/element.ts with a side-effect-free GrigsonChart class. Created src/register.ts for auto-registration of the 'grigson-chart' element. Exported GrigsonChart from index.browser.ts. Updated vite.config.ts to use VITE_BUILD_REGISTER environment variable to switch between core and register bundles. Updated package.json with build:browser:register script. Verified that pnpm build produces both grigson.iife.js and grigson-register.iife.js. All 122 tests pass.

## custom-element-rendering
Implemented the \`GrigsonChart\` custom element to parse chart content from a child \`<template>\` and render it into a Shadow Root using the \`TextRenderer\`. The \`connectedCallback\` defers the initial \`update()\` to ensure children are available. Added 5 tests in \`src/element.test.ts\` using \`happy-dom\` (installed as a devDependency in \`packages/grigson\`) covering rendering, empty template, error handling, and manual updates. Updated \`browser-bundle.md\` with the correct usage example. All 127 tests pass.

## custom-element-attributes
Implemented \`observedAttributes\` and \`attributeChangedCallback\` in \`GrigsonChart\` to handle \`renderer\`, \`transpose-key\`, \`transpose-semitones\`, and \`notation-preset\`. Updated \`TextRenderer\` to accept a \`TextRendererConfig\` object and support notation presets (\`jazz\`, \`pop\`, \`symbolic\`) for chord suffixes. Verified reactivity with new tests in \`element.test.ts\` showing that changing the \`notation-preset\` attribute via \`setAttribute\` correctly re-renders the chart with the new styling. Updated \`browser-bundle.md\` and \`renderer.md\` documentation. All 129 tests pass.

## custom-element-styling
Created \`HtmlRenderer\` in \`src/renderers/html.ts\` which produces semantic HTML with \`part\` attributes (\`song\`, \`frontmatter\`, \`row\`, \`barline\`, \`chord\`, \`chord-root\`, \`chord-suffix\`). Updated \`GrigsonChart\` custom element to use \`HtmlRenderer\` and added a default stylesheet in the Shadow Root that supports light/dark mode via \`prefers-color-scheme\` and exposes CSS Custom Properties for colors and fonts. Updated \`element.test.ts\` to verify HTML rendering and styling capabilities. All 132 tests pass.

## detect-key-ending-wins
Added an ending-key-wins rule to detectKey in src/theory/keyDetector.ts. When a progression ends with a V7→tonic cadence (dominant7 penultimate chord resolving to a major or minor tonic as the final chord), and the resolved tonic is the root of a candidate key that scored within 1 point of the overall best-scoring key, that cadence key is promoted as the result. The rule requires an authentic V7→I cadence to fire (penultimate chord quality = dominant7, and its root must be exactly a fifth above the candidate key's tonic) — this prevents simple diatonic final chords from incorrectly overriding a clear winner. Added 3 tests in a new 'ending-key-wins' describe block: the Whisper Not A-section [Cm, D7, Gm, A7, Dm] → 'Dm' (new), and two confirmations that existing logic is preserved. All 136 tests pass.

## website-custom-element-dogfood
Added \`normalise\` attribute support to the \`GrigsonChart\` custom element to enable automatic enharmonic correction. Updated \`packages/website/.eleventy.js\` to passthrough the \`grigson-register.iife.js\` bundle. Loaded the register bundle in \`base.njk\`. Refactored \`demo.md\` to use \`<grigson-chart normalise>\`. Refactored \`playground.njk\` to use \`<grigson-chart id="playground-output" normalise>\` for output, removing the manual Shiki rendering while maintaining Monaco for input. All 133 tests pass.

## section-parsing
Added a `Section` interface to `src/parser/types.ts` with `{ type: 'section'; label: string | null; rows: Row[] }` and replaced `Song.rows: Row[]` with `Song.sections: Section[]`. Extended the Peggy grammar (`grammar.pegjs`) with a `SectionLabel` rule matching `[Label]` lines as top-level song body elements; the updated `SongBody` action groups rows into Section nodes using a pending-label accumulator. Updated `TextRenderer` to emit section labels on their own line (skipping the line when `label` is null). Updated `HtmlRenderer` to wrap each section in a `<div part="section">` with an optional `<div part="section-label">`. Updated `normaliseSong` in `normalise.ts` to iterate `song.sections` instead of `song.rows`, importing the `Section` type. Exported `Section` from `index.ts` and `index.browser.ts`. Updated all existing tests in `parser.test.ts` and `normalise.test.ts` that accessed `song.rows` to use `song.sections[0].rows`. Added `src/parser/section.test.ts` with 7 tests covering: no-label songs produce one section with null label, two-label songs produce two sections, blank lines don't create extra sections, TextRenderer emits labels correctly, and round-trip parse→render→parse produces equal ASTs. All 143 tests pass.

## normalise-per-section
Refactored normaliseSong to operate per section with independent key detection.

## harmonic-analysis-core
Created src/theory/harmonicAnalysis.ts exporting the AnnotatedChord interface ({ chord, homeKey, currentKey, currentKeyCandidates }) and analyseHarmony(chords, homeKey). The function performs a left-to-right sliding window scan with two pattern detectors: (1) 2-5-1 — a minor or half-diminished ii chord followed by a dominant7 V7 a perfect 4th above, followed by a tonic I a perfect 5th below the V7; all three get currentKey set to the resolved tonic key; (2) 5-1 — a dominant7 followed by a tonic a perfect 5th below; both get the resolved tonic key. 2-5-1 is checked first at each position, giving it natural priority. Key names are resolved from pitch class via two lookup maps (major and minor) built from the KEYS record, with Gb preferred over F# for pitch class 6. Exported analyseHarmony and AnnotatedChord from index.ts. Added 7 tests covering: all-homeKey baseline, 5-1 detection (A7→Dm, G7→C), 2-5-1 detection (Bbm-Eb7-Ab in C context), 2-5-1 in Db (Ebm-Ab7-Db in F context), 2-5-1 priority over 5-1, half-diminished ii in 2-5-1, and homeKey preservation. All 154 tests pass.

## harmonic-analysis-isolated
Extended analyseHarmony with a circle-of-fifths fallback for isolated borrowed chords. Added circleOfFifthsDistance(keyA, keyB): number (exported) which maps minor keys to their relative major and computes the minimum angular distance on the 12-position circle of fifths. For a chord not covered by a 2-5-1 or 5-1 pattern and not diatonic to homeKey, analyseHarmony now finds all in-scope keys whose diatonic note set contains the chord root (by spelling), sorts them by circleOfFifthsDistance from homeKey, and picks the closest; when the homeKey's parallel minor (e.g. Cm for homeKey C) is among the tied closest candidates it is preferred, otherwise the first major key in the tied set is chosen. Added 5 new tests: isolated bVI (Ab in C → Cm), isolated bVII (Bb in C → F), isolated Bb7 (in C → F), diatonic chord stays homeKey, and circleOfFifthsDistance spot-checks. All 159 tests pass.
## transpose-rewrite
Created src/theory/transpose.ts exporting three functions. transposeSection(chords, semitones) shifts every chord root's pitch class by the semitone delta (mod 12) using a flat-side intermediate spelling (C Db D Eb … B), then delegates all enharmonic spelling to normaliseSection — no spelling logic lives in transpose.ts. transposeSong(song, semitones) applies transposeSection per section (via the internal helper that returns both the normalised chords and the homeKey), rebuilds the section rows from the transposed chord list, and sets song.key to the first section's homeKey. transposeSongToKey(song, targetKey) detects the home key of the first section, computes the semitone interval to the target key's tonic pitch class, and calls transposeSong. Exported all three functions from index.ts and index.browser.ts. Re-wired transpose-key and transpose-semitones attributes in element.ts: if transpose-key is set, calls transposeSongToKey; if transpose-semitones is set, calls transposeSong. Added 6 tests in src/theory/transpose.test.ts covering: C major +7 → G major, Bb major +2 → C major, borrowed bVII (Bb in C +7 → F not E#), borrowed bVI (Ab in C +7 → Eb not D#), 2-5-1 transposition with correct flat spelling (Bb not A#), and transposeSongToKey round-trip equivalence. All 170 tests pass.

## cli-transpose
Implemented the `grigson transpose` subcommand in src/cli.ts. Added `runTranspose()` which reads input from a file argument or stdin, dispatches to `transposeSongToKey` (for `--to`) or `transposeSong` (for `--raise`/`--lower`), and writes normalised output to stdout. Used `--raise <n>` and `--lower <n>` (both positive integers) instead of `--semitones <n>` (positive or negative) to avoid a minimist flag-parsing ambiguity where `-3` is treated as the flag `-3` rather than the value of `--semitones`. Exits with code 1 if more than one of the three flags is provided or if none is provided. Added 6 acceptance tests covering all scenarios. Updated documentation/cli.md and the prd.json detail/steps. All 176 tests pass.

## chord-qualities-parse-render
Added four new Quality union members to src/parser/types.ts: 'diminished', 'maj7', 'min7', 'dim7'. Extended the Peggy grammar (grammar.pegjs) with new alternatives in PEG-safe order: m7b5 > maj7 > M7 > dim7 > m7 > dim > m > 7 > "" — longer alternatives precede shorter ones to avoid premature matches. CM7 and Cmaj7 both produce quality 'maj7'; canonical output is always 'maj7'. Extended TextRenderer.renderChord() with a switch statement covering all eight qualities (dim → 'dim', maj7 → 'maj7', min7 → 'm7', dim7 → 'dim7'). Updated the old "unsupported qualities" test block — Cm7, CM7, and Cdim are now supported; replaced with tests for truly unsupported suffixes (sus4, aug). Added 9 parser tests covering the four new qualities (including CM7 normalisation to maj7) and 9 renderer tests (render + round-trip for each new quality). All 194 tests pass.

## chord-qualities-key-detector
Replaced MAJOR_DEGREE_QUALITIES and HARMONIC_MINOR_DEGREE_QUALITIES (single-Quality-per-degree arrays) with MAJOR_DEGREE_QUALITY_SETS and HARMONIC_MINOR_DEGREE_QUALITY_SETS (Set<Quality>[] covering both triad and tetrad per degree) in src/theory/keyDetector.ts. Updated buildKeyScoreMap to return Map<number, Set<Quality>>. Replaced qualityMatches with qualityMatchesSet using Set.has(). Removed the explicit dominant7→major special case — now handled naturally by the V degree set { 'major', 'dominant7' }. Updated computeScore to call qualityMatchesSet with the Set from the map. All 194 existing tests pass with no regressions.

## normalise-harmonic
Rewrote normaliseSection in src/theory/normalise.ts to call analyseHarmony and use each chord's currentKey for enharmonic spelling. The updated normaliseChord now takes two pcToNote maps: (1) homePCToNote from the detected homeKey — used first for any pitch class that is diatonic to the homeKey (handles enharmonic corrections like C#→Db in Db major); (2) currentPCToNote from the analyseHarmony-assigned currentKey — used for pitch classes not in homeKey (handles borrowed chords like Bb in C major being spelled from F major's note set, or Eb7 in a 2-5-1 to Ab being spelled from Ab's note set). Removed the old FLAT_SPELLING constant and preferredNonDiatonicSpelling next-chord heuristic entirely. Added 5 new tests in a 'normalise-harmonic-based spelling' describe block: 2-5-1 Bbm-Eb7-Ab in C, 2-5-1 Ebm-Ab7-Db in F (What's New B section), borrowed Bb bVII in C, secondary dominant A7 in C, and Dm7b5-G7-Am-Am. All existing Cat 2, 5, 8 tests continue to pass. All 164 tests pass.

## chord-qualities-harmonic-analysis
Extended analyseHarmony in src/theory/harmonicAnalysis.ts to recognise min7 and diminished qualities in 2-5-1 and 5-1 pattern matching. The 2-5-1 ii-chord quality check now accepts 'minor', 'min7', 'halfDiminished', and 'diminished'. The iIsMinor tonic-resolution check now accepts 'minor', 'halfDiminished', and 'min7' (so min7 chords resolve to minor keys; maj7 and major resolve to major keys). Added a new 'analyseHarmony — tetrad quality support' describe block with 4 tests: Dm7-G7-Cmaj7 → 2-5-1 in C, Bm7b5-E7-Am7 → 2-5-1 in Am, G7-Cmaj7 → 5-1 in C, Bdim-E7-Am → 2-5-1 in Am (diminished triad ii). All 204 tests pass.

## chord-qualities-tetrad-tests
Added `maj7`, `min7`, and `dim7` chord helper functions to src/theory/keyDetector.test.ts. Added a new 'detectKey — tetrad quality disambiguation' describe block with 6 tests demonstrating that tetrad qualities resolve ambiguous cases that triads cannot: (1) Cmaj7/Gmaj7 alternation → G (Gmaj7 misses C's V-set {major, dominant7}); (2) Cmaj7/G7 alternation → C (G7 misses G's I-set {major, maj7}); (3) full ii-V-I-vi in C with tetrads (Dm7, G7, Cmaj7, Am7 → C); (4) ii-V-I in D (Em7, A7, Dmaj7 → D); (5) iiø-V7-i in Am with tetrads (Bm7b5, E7, Am7 → Am via V7-of-minor tiebreak); (6) Am harmonic minor with G#dim7 VIIdim7 as dominant substitute → Am via last-chord tonic tiebreak. The PRD step listed Gdim7 but the comment specified G#dim7 (the diatonic VIIdim7 of Am); used G#dim7 since Gdim7 is non-diatonic to Am and would break the threshold. No implementation changes were needed — the existing Set<Quality>-based scorer handles all cases correctly. All 200 tests pass.

## chord-qualities-normalise
Added a new 'normaliseSong — chord-qualities smoke tests' describe block in src/theory/normalise.test.ts with 4 tests verifying that normalisation handles the new chord qualities end-to-end: (1) [Dm7, G7, Cmaj7] in C — all roots already correctly spelled, qualities pass through unchanged; (2) [D#m7, G#7, C#maj7] — detectKey returns 'Db', 2-5-1 harmonic analysis assigns currentKey='Db' to all three, roots rewritten to Ebm7, Ab7, Dbmaj7 with qualities preserved; (3) [Bm7b5, E7, Am7] — 2-5-1 resolves to Am, all roots (B, E, A) already canonical for Am, no rewrites; (4) [Am7, Dm7, G#dim7, Am7] — detectKey returns 'Am', G# is the diatonic raised VII of Am harmonic minor so G#dim7 root is preserved as G#. No implementation changes were needed — the normaliser correctly delegates to analyseHarmony which already supports min7 and diminished in 2-5-1 patterns. All 208 tests pass.

## textmate-grammar-chord-qualities
Updated the chord quality alternation in the 'chord' repository item regex in packages/textmate-grammar/grigson.tmLanguage.json from 'm7b5|m|7' to 'm7b5|maj7|M7|dim7|m7|dim|m|7', ensuring longer alternatives precede shorter ones to avoid premature matches (dim7 before dim, maj7 before m7, m7b5 before m7 before m). Synced the updated grammar to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. Added two new rows to packages/grigson/documentation/examples/syntax-test.chart covering all new quality suffixes: Cdim, Cmaj7, CM7, Dm7, Bdim7, G#dim7, Am7, Fmaj7. All 208 existing tests pass with no regressions.

## validator-core
Created src/validator.ts exporting DiagnosticRange, Diagnostic interfaces and a validate(source: string): Diagnostic[] function. The function calls parseSong() and returns [] for valid input. For Peggy parse errors it uses an isPeggyError type guard to check for the location property, then maps the 1-indexed Peggy line/column to 0-indexed LSP-style range values (subtracting 1 from each). Non-Peggy errors fall back to a zero range with String(e) as the message. Exported validate, Diagnostic, and DiagnosticRange from packages/grigson/src/index.ts. Added src/validator.test.ts with 7 tests: empty file valid, minimal chart valid, unsupported suffix returns one error, garbage input returns one error, range values are non-negative integers, valid chart with front matter returns [], and error location reflects the correct line for multi-line input. All 215 tests pass.

## cli-validate
Implemented the `grigson validate` subcommand in src/cli.ts. Added `runValidate()` which reads from one or more file arguments or stdin (labelled `<stdin>`), calls `validate()` on each, and reports diagnostics. Default text output uses the standard Unix linter format `<file>:<line>:<character>: <severity>: <message>` with 1-indexed line and character numbers. `--format json` emits a JSON array of `{ file, line, character, severity, message }` entries. Exits with code 0 when all inputs are valid, code 1 when any error is found. Registered `validate` in the SUBCOMMANDS tuple, dispatch switch, and all help strings. Added 5 acceptance tests in `src/cli.test.ts` covering: valid file exits 0, invalid file exits 1 with filename+line, stdin reading, JSON empty array on valid input, and JSON array with typed error entry on invalid input. Updated `documentation/cli.md` with a full `grigson validate` section including text and JSON output format descriptions and usage examples. All 220 tests pass.

## language-server
Created packages/language-server/ as a new workspace package (already covered by pnpm-workspace.yaml's 'packages/*' glob). package.json sets name 'grigson-language-server' with bin pointing to ./dist/server.js. Dependencies: vscode-languageserver, vscode-languageserver-textdocument, and grigson (workspace:*). Because vscode-languageserver is a CommonJS package and grigson is ESM, server.ts uses a dynamic import() for grigson at startup and CommonJS module output via tsconfig.json (module: CommonJS, moduleResolution: Node). The build script compiles with tsc and prepends a #!/usr/bin/env node shebang via a Node.js one-liner. The server calls validate() on textDocument/didOpen and textDocument/didChange events and sends diagnostics via connection.sendDiagnostics(). Smoke-tested by sending an LSP initialize message via stdin and confirming the capabilities response. Created documentation/language-server.md with usage instructions and per-editor configuration for VS Code, Neovim, Helix, and Emacs. All 220 existing tests continue to pass.

## vscode-extension-lsp
Extended packages/vscode-extension/ to act as a thin LSP client for the language server. Updated package.json with activationEvents: ["onLanguage:grigson"], main: "./dist/extension.js", vscode-languageclient dependency, @types/vscode devDependency, and a build script. Created packages/vscode-extension/tsconfig.json targeting ES2020/Node16 with outDir: dist. Created packages/vscode-extension/src/extension.ts with activate() and deactivate() functions using LanguageClient from vscode-languageclient/node. The activate function resolves the server module from the sibling packages/language-server/dist/server.js path (monorepo layout) and starts an LSP client with both run and debug server options. The client registers a subscription via context.subscriptions.push(client) so VS Code disposes it on extension deactivation. Ran pnpm build in the extension package — dist/extension.js produced without errors. All 220 existing tests continue to pass.

## time-signature-parsing
Added TimeSignature interface ({ numerator: number; denominator: number }) and an optional timeSignature?: TimeSignature field to the Bar interface in types.ts. Extended the Peggy grammar with a TimeSignatureToken rule matching (N/D) tokens immediately after a barline and before chord content; both BarTail and Bar rules now optionally parse this token and include it in the returned bar object. Updated TextRenderer.renderBar() to emit '(N/D) ' before the chord text when bar.timeSignature is defined. Exported TimeSignature from parser.ts, index.ts, and index.browser.ts. Created src/parser/timeSignature.test.ts with 8 tests covering: (4/4), (3/4), (6/8) parsing, absent time signature, two-bar row with only first bar having a time signature, renderer emission, no-prefix when absent, and a round-trip test. All 228 tests pass.

## beat-slot-parsing
Replaced Bar.chord: Chord with Bar.slots: BeatSlot[] — a breaking AST change. Added ChordSlot ({ type: 'chord'; chord: Chord }), DotSlot ({ type: 'dot' }), and BeatSlot (ChordSlot | DotSlot) to types.ts. Extended the Peggy grammar with BeatSlotList (one or more whitespace-separated BeatSlot items) and BeatSlot (Chord or '.') rules; BarTail and Bar now use BeatSlotList; bars with no ChordSlot are rejected via error(). Added '_' before TimeSignatureToken? in Bar to handle leading whitespace. Updated TextRenderer.renderBar() to map slots to chord text or '.', joined by spaces. Updated HtmlRenderer.renderBar() similarly, using <span part="dot">.</span> for dots. Updated normalise.ts and transpose.ts to extract Chord[] by filtering ChordSlots and to write normalised/transposed chords back into ChordSlots while preserving DotSlots. Exported ChordSlot, DotSlot, BeatSlot from parser.ts, index.ts, and index.browser.ts. Updated all existing tests (parser.test.ts, section.test.ts, normalise.test.ts, transpose.test.ts) to use the new slots structure. Created src/parser/beatSlot.test.ts with 11 tests covering single/multi-chord bars, dot combinations, dot-only rejection, round-trips, and rendering. All 239 tests pass.

## validate-beat-balance
Added a semanticChecks(song: Song): Diagnostic[] helper to src/validator.ts. The helper iterates sections → rows → bars, carrying forward the effective time signature (defaulting to 4/4). For mode-2 bars (any DotSlot present), it compares the slot count against the effective numerator and emits a severity: 'warning' Diagnostic when they differ. Mode-1 bars (no dots) are never warned about. Updated validate() to call parseSong() and then semanticChecks(), concatenating results. Added 7 tests in a new 'validate — beat balance' describe block covering: balanced 3/4, balanced 4/4, underfilled 5/4, overfilled 4/4, mode-1 no-warning, per-bar time-signature change, and inherited time signature. Updated documentation/validator.md with a Semantic checks section. All 246 tests pass.

## tree-sitter-chord-grammar
Extended grammar.js with note_letter, accidental, quality, and chord rules. note_letter matches /[A-G]/. accidental and quality use token.immediate() so no whitespace can appear between the root letter and its suffix. quality alternatives in longest-first order: m7b5, maj7, M7, dim7, m7, dim, m, 7. A chord is seq(note_letter, optional(accidental), optional(quality)). Changed extras to /[ \t\r\n]/ (all whitespace as extras) and updated song to repeat($.chord) as a temporary placeholder for chord-level testing — newlines will become significant again when row rules are added. Created test/corpus/chords.txt with 10 corpus tests covering C (natural), F# (sharp), Bb (flat), Am (minor), G7 (dominant7), Bm7b5 (half-diminished), Cmaj7, CM7, Dm7 (min7), Bdim7 (dim7). All 10 tree-sitter corpus tests pass; all 246 existing Vitest tests pass.

## tree-sitter-bar-grammar
Extended grammar.js with integer, barline, time_signature, bar_tail, and row rules. Changed extras to /[ \t]/ (spaces and tabs only) so newlines are now significant. song is now repeat(choice($.row, /\r?\n/)). barline is the literal '|'. integer matches /[0-9]+/. time_signature is seq('(', $.integer, '/', $.integer, ')'). bar_tail is seq(optional($.time_signature), $.chord, $.barline). row is seq($.barline, repeat1($.bar_tail)). Updated test/corpus/chords.txt to wrap each chord in a row (| chord |) since bare chords are no longer valid at the song level. Created test/corpus/bars.txt with 6 corpus tests: single-bar row, four-bar row, bar with (4/4) time signature, first bar with time signature and second without, (6/8) time signature, and mixed accidentals and qualities. All 16 tree-sitter corpus tests pass; all 246 Vitest tests continue to pass.

## tree-sitter-frontmatter-grammar
Added frontmatter_delimiter (/---[ \t]*\r?\n/), frontmatter_key (/[a-zA-Z]+/), frontmatter_value (/[^\r\n]+/), frontmatter_field (seq of key, colon, value, newline), and frontmatter (seq of delimiter, repeat fields, delimiter) rules to grammar.js. Updated song from repeat(choice($.row, /\r?\n/)) to repeat(choice($.frontmatter, $.row, /\r?\n/)) — using a flat repeat rather than seq(optional(frontmatter), ...) to avoid a reduce/reduce conflict caused by two consecutive repeats both matching newlines. Ran tree-sitter generate. Created test/corpus/frontmatter.txt with 6 corpus tests: title field, title+key fields, quoted value, unquoted value with F#, empty frontmatter, and frontmatter followed by a row. All 22 tree-sitter corpus tests pass; all 246 Vitest tests continue to pass.

## tree-sitter-scaffold
Created packages/tree-sitter-grammar/ as a new workspace package. package.json sets name 'tree-sitter-grigson' with scripts generate/build/test all invoking tree-sitter CLI, devDependency tree-sitter-cli ^0.25.0, and a "tree-sitter" field declaring scope "source.grigson" and file-types ["chart"]. grammar.js contains a minimal skeleton: song rule matches zero or more lines via repeat(/[^\n]*\n/). Ran tree-sitter generate to produce src/parser.c, src/grammar.json, src/node-types.json, and src/tree_sitter/ — all committed. Added pnpm.onlyBuiltDependencies config to root package.json so tree-sitter-cli's postinstall download script runs on pnpm install. The **/node_modules/ gitignore pattern already excludes tree-sitter-grammar/node_modules/. All 246 existing tests continue to pass.

## tree-sitter-section-grammar
Added section_name (/[^\]\r\n]+/) and section_label (seq('[', $.section_name, ']')) rules to grammar.js. Updated song from repeat(choice($.frontmatter, $.row, /\r?\n/)) to seq(optional($.frontmatter), repeat(choice($.section_label, $.row, /\r?\n/))) — this puts frontmatter in an optional prefix position and adds section_label as a top-level song body element. The optional() approach required removing the leading blank line from each frontmatter corpus test (the blank line caused a conflict where /\r?\n/ was consumed before optional frontmatter could match). Ran tree-sitter generate. Created test/corpus/sections.txt with 4 corpus tests: [A] produces section_label with section_name, [Chorus] produces section_label with section_name Chorus, two labels produce two section_label nodes, section_label immediately followed by a row. Created test/corpus/song.txt with a full integration test: frontmatter + [A] + two rows + blank line + [B] + two rows — the tree has frontmatter, section_label("A"), row, row, section_label("B"), row, row as top-level children of song. All 27 tree-sitter corpus tests pass; all 246 Vitest tests continue to pass.

## tree-sitter-highlight-queries
Created queries/highlights.scm in packages/tree-sitter-grammar/ mapping all named nodes to standard tree-sitter capture names: frontmatter_delimiter → @punctuation.special; frontmatter_key → @variable.member; frontmatter_value → @string; section_label brackets → @punctuation.bracket; section_name → @markup.heading; barline → @punctuation.delimiter; time_signature parentheses → @punctuation.bracket, slash → @operator, integers → @number; chord note_letter → @constant; chord accidental → @operator; chord quality → @keyword. Also created tree-sitter.json (required by tree-sitter CLI v0.25 for ABI 15 and for highlight query discovery) with grammars and metadata fields. Ran tree-sitter generate + build + highlight against a sample .chart file — all node types highlighted with no ERROR nodes. All 27 tree-sitter corpus tests pass; all 246 Vitest tests pass.

## tree-sitter-integration-docs
Created editor integration documentation for Neovim and Helix in packages/tree-sitter-grammar/README.md (merged into the package README rather than a separate docs file). Content covers: tree-sitter vs LSP comparison table; Neovim Path A (TSInstall after upstream nvim-treesitter PR) with BufRead autocmd; Neovim Path B (manual parser_config registration via get_parser_configs() before upstream acceptance, with highlights.scm placement); Helix [[grammar]]/[[language]] stanzas and hx --grammar build step; and a note that tree-sitter and LSP run side-by-side. Updated packages/language-server/README.md with a diagnostics section and tree-sitter cross-reference. Removed packages/grigson/documentation/tree-sitter.md and documentation/language-server.md (content now lives in the respective package READMEs). Updated root README.md and packages/grigson/README.md links accordingly. All 246 tests pass.

## sync-textmate-dot-beat-slot
Added a 'dot' repository entry to packages/textmate-grammar/grigson.tmLanguage.json with match `(?<=\||\s)\.(?=\s|\||$)` and scope `punctuation.separator.beat.grigson`. Included `#dot` in the main pattern list after `#barline` and before `#chord`. Synced the same change to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. All 246 tests pass.

## sync-textmate-time-signature
Added a 'time-signature' repository entry to packages/textmate-grammar/grigson.tmLanguage.json with match `(\()(\d+)(/)(\d+)(\))` and five capture groups: opening paren → punctuation.section.time-signature.begin.grigson, numerator → constant.numeric.grigson, slash → punctuation.separator.grigson, denominator → constant.numeric.grigson, closing paren → punctuation.section.time-signature.end.grigson. Included `#time-signature` in the main pattern list after `#barline` and before `#dot`. Synced the same change to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. All 246 tests pass.

## sync-tree-sitter-beat-slot-list
Added `dot` (`'.'`) and `beat_slot` (`choice($.chord, $.dot)`) rules to packages/tree-sitter-grammar/grammar.js. Updated `bar_tail` to use `seq(optional($.time_signature), repeat1($.beat_slot), $.barline)` instead of the single `$.chord`. Regenerated the C parser with `tree-sitter generate`. Updated all five corpus test files (bars.txt, chords.txt, frontmatter.txt, sections.txt, song.txt) to wrap chord nodes inside `beat_slot` nodes; added 3 new bar corpus tests covering multiple chords per bar, chord-dot-chord, and time-signature with multiple beat slots. Added `(beat_slot (dot) @punctuation.separator)` capture to queries/highlights.scm. All 30 tree-sitter corpus tests pass; all 246 Vitest tests pass.

## sync-tree-sitter-frontmatter-quoted-value
Added `quoted_string` (`/"[^"]*""/`) and `unquoted_value` (`/[^\r\n\t "][^\r\n]*/`) rules to grammar.js. Replaced the old single-regex `frontmatter_value` with `frontmatter_value: ($) => choice($.quoted_string, $.unquoted_value)`. The `unquoted_value` regex starts with `[^\r\n\t "]` (excluding leading whitespace and quotes) to prevent a GLR ambiguity where a space before a quoted value could be greedily consumed as a spurious unquoted_value token. Updated `queries/highlights.scm` to scope both `(quoted_string)` and `(unquoted_value)` as `@string`. Regenerated the C parser with `tree-sitter generate`. Updated all corpus tests (frontmatter.txt, song.txt) to show the new child node type inside `frontmatter_value`; the existing "Frontmatter with quoted value" test now correctly expects `(frontmatter_value (quoted_string))`. All 30 tree-sitter corpus tests pass; all 246 Vitest tests pass.

## minor-keys-exotic-note-names
Added 'E#': 5, 'B#': 0, 'Cb': 11, and 'Fb': 4 to NOTE_MAP in src/theory/pitchClass.ts. These enharmonic spellings are needed by the KEYS table entries for G#m (E# as raised 7th is skipped, B as natural — but Cb and Fb appear in Abm and Ebm respectively). Added 4 unit tests in pitchClass.test.ts asserting rootToPitchClass returns the correct pitch class for each new spelling. All 247 tests pass.

## minor-keys-add
Added Abm, G#m, Ebm, and D#m to the KEYS table in src/theory/keys.ts. Scale spellings: Abm = [Ab, Bb, Cb, Db, Eb, Fb, G] (no relative — Cb major not in KEYS); G#m = [G#, A#, B, C#, D#, E, G] with relative 'B'; Ebm = [Eb, F, Gb, Ab, Bb, Cb, D] with relative 'Gb'; D#m = [D#, E#, F#, G#, A#, B, D] with relative 'F#'. Added 'G#m'↔'Abm' and 'D#m'↔'Ebm' to ENHARMONIC_PAIRS in pitchClass.ts. Updated the KEYS count test from 23 to 27. Added 4 normalisation tests: G#m and D#m tests use V7 progressions that auto-detect correctly; Abm and Ebm tests use forceKey (auto-detection tiebreaking for these enharmonic pairs is handled in the minor-keys-detection task). All 252 tests pass.

## minor-keys-detection
Added breakGSharpAbTie and breakDSharpEbTie tiebreaker functions to keyDetector.ts, mirroring the existing breakFSharpGbTie pattern. Each function checks config preference first, then counts sharp-side vs flat-side chord root spellings across the shared enharmonic pitch classes (G#/Ab/D# for G#m/Abm; D#/A#/G# vs Eb/Bb/Ab for D#m/Ebm), defaulting to the flat side if tied. Extended DetectKeyConfig with gSharpOrAFlat and dSharpOrEFlat fields. Wired both tiebreakers into detectKey after the F#/Gb block. Fixed the ending-key-wins block to skip enharmonic equivalents of bestKey (preventing Abm from overriding G#m via the cadence heuristic after the tiebreaker already resolved it). Extended CLI --enharmonic to accept g-sharp, a-flat, d-sharp, e-flat. Updated cli.md with a full enharmonic values table. Added 8 key-detection tests (4 per enharmonic pair). All 260 tests pass.

## dorian-key-helpers
Added KeyMode type ('major' | 'minor' | 'dorian') and getKeyMode(key): KeyMode and getKeyRoot(key): string helpers to src/theory/keys.ts. getKeyMode returns 'dorian' for keys ending with ' dorian', 'minor' for keys ending with 'm', and 'major' otherwise. getKeyRoot strips the ' dorian' suffix (7 chars), trailing 'm', or returns the key unchanged for major. Exported both from keys.ts and from index.ts. Replaced all raw endsWith('m') / slice(0,-1) / replace(/m$/, '') call sites: in keyDetector.ts (buildKeyScoreMap, breakRelativeTie, detectKey tie-breaker, and ending-key-wins block), in harmonicAnalysis.ts (circleOfFifthsDistance getPosition helper, buildKeyMaps, and borrowed-chord fallback), and in transpose.ts (transposeSongToKey fallback). Pure refactor — no behaviour changes. All 260 tests pass.

## dorian-keys-table
Added 12 dorian mode entries to KEYS in src/theory/keys.ts using the naming convention 'X dorian'. Scale formula: natural minor with raised 6th ([0,2,3,5,7,9,10] semitones). Each entry has a relative field pointing to the major key whose tonic is one whole step below the dorian root (e.g. 'E dorian' → relative: 'D'). Keys added: C, D, E, F, G, A, B, Bb, Eb, F#, Ab, C# dorian. Updated KEYS count test from 27 to 39. Added 5 tests to keys.test.ts: diatonicNotes for E dorian and B dorian, relative field for E dorian, and getKeyMode/getKeyRoot for dorian keys. Also fixed two bugs in keyDetector.ts triggered by the new entries: (1) buildKeyScoreMap now returns an empty map for dorian keys (scoring not yet implemented, avoids false ties); (2) ending-key-wins block now uses getKeyMode(key) !== 'major' instead of === 'minor' so dorian tonic chords don't incorrectly match a major-quality cadence. All 261 tests pass.

## dorian-detection-tiebreak
Added `breakDorianMajorTie(dorian, major, chords): string` to keyDetector.ts. Tiebreak priority: (1) V7 of the relative major present → major; (2) IV→i dorian plagal cadence (consecutive major IV followed by minor i) → dorian; (3) first-chord quality at tonic; (4) last-chord quality at tonic; (5) default major. Note: first chord is checked before last chord (reversing the originally planned order) to fix a regression in T9-b where [Gb, Db, Ebm, Abm] would incorrectly prefer Ab dorian (whose tonic Ab appears as the last chord) over Gb major (whose tonic Gb appears as the first chord). Wired into detectKey after the enharmonic tiebreak blocks: if bestKey is dorian and its relative tied, call breakDorianMajorTie; if bestKey is major and any dorian key whose relative === bestKey tied, call breakDorianMajorTie. Added 4 tests: [Em, G, D, A] → 'E dorian' (first-chord minor tonic fires), [C, Em, Am, G] → 'C' (first-chord major tonic fires), [Dm, C, G, Dm] → 'D dorian' (G→Dm plagal cadence fires), and declared-key preservation. All 267 tests pass.

## dorian-detection-scoring
Added DORIAN_INTERVALS = [0,2,3,5,7,9,10] and DORIAN_DEGREE_QUALITY_SETS to keyDetector.ts. Quality sets: i=minor/min7, ii=minor/min7, bIII=major/maj7, IV=major/maj7, v=minor/min7, vi°=diminished/halfDiminished, bVII=major/maj7. Note: dominant7 was intentionally excluded from IV and bVII — including it caused false positives where dorian keys outscore their relative major (e.g. D dorian beating C major for [Dm7, G7, Cmaj7]) before the dorian/relative-major tiebreak is wired. Updated buildKeyScoreMap to select dorian tables when getKeyMode(key) === 'dorian', replacing the early-return empty-map stub. Fixed breakRelativeTie to use getKeyRoot(major) instead of raw major for the PC lookup. Fixed detectKey to skip relative tiebreak when either key is dorian (dorian tiebreak is a separate task). Fixed ending-key-wins cadence check to use getKeyMode(key) === 'minor' (not !== 'major') so dorian keys don't falsely match minor-quality cadences. Added 2 new tests: [Bm, A, E] → 'B dorian' (B harmonic minor scores only 3 vs 6) and [Em, G, D, A] → 'E dorian' (E harmonic minor scores poorly, A major quality mismatch). All 263 tests pass.

## dorian-cof-distance
The circleOfFifthsDistance function in harmonicAnalysis.ts already handled dorian keys (added as part of dorian-key-helpers) — line 29 checks mode === 'dorian' and maps to the relative major, identical to the minor key handling. Added 3 missing tests to harmonicAnalysis.test.ts: circleOfFifthsDistance('E dorian', 'D') === 0 (E dorian's relative is D), circleOfFifthsDistance('E dorian', 'G') === 1, and circleOfFifthsDistance('E dorian', 'C') === 2. All 267 tests pass.

## dorian-harmonic-key-maps
Extended buildKeyMaps in harmonicAnalysis.ts to build a dorianByPC: Map<number, string> alongside majorByPC and minorByPC, iterating KEYS entries where getKeyMode === 'dorian'. Exported DORIAN_BY_PC, MAJOR_BY_PC, and MINOR_BY_PC constants. Updated resolveKey to fall back to DORIAN_BY_PC when iChordIsMinor is true but no minor key exists for the tonic pitch class — allowing a 2-5-1 or 5-1 tonicisation to resolve to a dorian centre. Added a test: analyseHarmony([Em, A, D, Em], 'E dorian') returns currentKey 'E dorian' for all chords (all diatonic). All 268 tests pass.

## dorian-plagal-cadence
Added a 3rd pattern detector to analyseHarmony in harmonicAnalysis.ts: the dorian plagal cadence (IV→i). The pattern fires only when getKeyMode(homeKey) === 'dorian', matches a major chord whose pitch class is (tonicPC + 5) % 12 (the IV) followed by a minor chord at tonicPC (the i), and assigns currentKey = homeKey to both chords. The block is placed after the 5-1 pattern so functional (2-5-1, 5-1) patterns take priority. Added 3 tests: G→Dm in D dorian, A→Em in E dorian, and F→C in C major (gate check — must not fire). All 271 tests pass.
## Richer Key Relationship Modeling

Replaced the `relative` field in `KeyInfo` with structured `scaleFamily`, `degree`, and `parent` fields that encode modal family membership explicitly.

- Added `ScaleFamily` type (`'major' | 'harmonic_minor'`), `degree` (1–7), and `parent` (scale family root) to `KeyInfo`; removed `relative`
- Updated all 39 KEYS entries with new structural fields
- Added `getSiblingModes(key)` — returns all KEYS entries in the same scale family
- Added `getRelativeMajor(key)` — maps any key to its ionian sibling; replaces all `KEYS[k].relative` lookups
- Migrated `harmonicAnalysis.ts` and `keyDetector.ts` callers to `getRelativeMajor`
- Exported `ScaleFamily`, `getSiblingModes`, `getRelativeMajor` from public API
- Updated tests (25 tests, all pass) and documentation

All 283 tests pass.
2026-03-16: Added aeolian (degree 6) and mixolydian (degree 5) mode support. 24 new KEYS entries (63 total). New scoring intervals/quality sets for each mode. Tiebreaker functions for aeolian/major and mixolydian/major ambiguity. bVII→i and bVII→I cadence patterns in analyseHarmony. Fixed ending-key-wins to skip modal keys. Updated tests and documentation.

## feat(meter): front-matter meter field with normalisation hoisting

- Added `meter: string | null` to FrontMatter and Song types
- Grammar: parse and validate meter (time signature pattern or "mixed")
- Normalise: hoist uniform inline meter to front-matter, strip inline tokens; mixed meter → "mixed" preserved inline
- TextRenderer: render meter in front-matter block
- 326 tests passing
- Commit: 0dcd69f


## parser-comment-lines
Added a Comment rule to grammar.pegjs that matches lines beginning with `#` outside front matter and discards them (returns null). Added Comment as the first alternative in SongBody alongside Newline/SectionLabel/Row. The existing null-filtering logic in the SongBody action (`typeof item !== 'object'` check) already handles null returns, so no action code changes were needed. Added 3 parser tests: single comment line, comment before section label, comment between rows. All 332 tests pass.

## parser-chord-qualities-dash-7b5
Added two new chord quality suffixes. Added `"7b5" { return "dom7flat5"; }` to the Quality rule in grammar.pegjs before `"7"`, and `"-" { return "min7"; }` before the empty fallback. Added `'dom7flat5'` to the Quality union type in types.ts. Added `case 'dom7flat5': suffix = '7b5'; break;` to renderChord in text.ts. Rebuilt with pnpm build. Added 5 parser tests (G7b5, F#7b5 → dom7flat5; C-, F#-, Bb- → min7) and 4 renderer tests (dom7flat5 renders as '7b5', C- renders as 'Cm7', plus round-trips for each). All 341 tests pass.

## parser-slash-chords
Added slash chord support (e.g. F/C, F#/A#, C-/Bb) to the parser, types, and renderer. Added `bass?: string` to the Chord interface in types.ts. Added a SlashBass rule to grammar.pegjs that matches "/" followed by a Root, and updated the Chord rule to capture an optional bass and include it in the returned object. Regenerated the parser with `pnpm build`. Updated renderChord in text.ts to append `'/' + chord.bass` when bass is present. Added 5 parser tests and 6 renderer/round-trip tests. All 353 tests pass.

## parser-extended-barlines
Added support for repeat and multi-barline types: `||`, `||.`, `||:`, `:||`, `:||xN`, `:||:`. Added `BarlineKind` union type and `Barline` interface to types.ts. Updated `Bar` with a required `closeBarline: Barline` field and `Row` with a required `openBarline: Barline` field. Replaced hardcoded `"|"` tokens in grammar.pegjs with `OpenBarline` and `CloseBarline` rules covering all six barline variants (single, double, final, startRepeat, endRepeat, endRepeatStartRepeat), with `:||xN` and `:||xN:` forms for repeat counts. Updated `Bar` rule to consume an `OpenBarline` and return the `closeBarline` on the bar. Added `barlineSymbol()` helper to text.ts and updated `renderRow()` to emit the correct barline symbols for open and close positions. Applied the same `barlineSymbol()` logic to html.ts. Exported `BarlineKind` and `Barline` from parser.ts, index.ts, and index.browser.ts. Updated all test helpers and existing test assertions in parser.test.ts, normalise.test.ts, and transpose.test.ts to include the new required barline fields. Created src/parser/barline.test.ts with 25 tests covering all barline kinds for open/close positions, multi-barline rows, renderer output, and round-trips. All 378 tests pass.

## parser-simile-mark
Added `%` simile mark support to the parser and renderer. In grammar.pegjs, added a second alternative to `BarTail` that matches `"%" _ close:CloseBarline _` and returns a temporary `{ type: "bar", simile: true, slots: [], closeBarline: close }` marker. Updated the `Row` action to resolve simile bars left-to-right: each simile bar's `slots` field is replaced with a deep copy of the preceding bar's `slots`, and the `simile` property is deleted before the Row is returned — so the resolved AST contains only `BeatSlot[]` with no trace of the parse-time marker. Added `simile?: { output?: 'shorthand' | 'longhand' }` to `TextRendererConfig` in text.ts. Added `slotsEqual()` helper and updated `renderRow()` to emit `%` for consecutive identical bars when `simile.output === 'shorthand'`; the first bar of a row is always rendered in full. Default remains `'longhand'` (always write chords out in full). Created src/parser/simile.test.ts with 13 tests covering: single `%`, multiple consecutive `%`, `%` after a multi-slot bar, time signature not carried by `%`, `%` with non-single closing barlines, no `simile` property in resolved AST, full-song parsing, renderer longhand default, renderer shorthand option, first-bar never shorthand, and shorthand round-trip. All 391 tests pass.

## parser-examples-regression-test
Created src/parser/examples.test.ts that uses readdirSync to enumerate all *.chart files in packages/grigson/documentation/examples/ and parses each with parseSong() inside an individual it() block. Added 16 tests (one per example file). All 407 tests pass.

## sync-textmate-qualities-7b5-dash
Updated the chord quality alternation in the 'chord' repository entry regex in packages/textmate-grammar/grigson.tmLanguage.json from 'm7b5|maj7|M7|dim7|m7|dim|m|7' to 'm7b5|maj7|M7|dim7|m7|dim|m|7b5|7|-', placing '7b5' before '7' (to avoid premature matching) and '-' at the end (Cm7 written as C-). Synced the same change to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. Added two new chords (G7b5, F#7b5, C-, Bb-) to packages/grigson/documentation/examples/syntax-test.chart. All 407 tests pass.

## sync-textmate-extended-barlines
Replaced the single-character `\\|` match in the 'barline' repository entry with a regex covering all eight barline variants in longest-first order: `:\\|\\|x\\d+:|:\\|\\|x\\d+|:\\|\\|:|:\\|\\||\\|\\|:|\\|\\|\\.|\\|\\||\\|`. This ensures :||x3:, :||x3, :||:, :||, ||:, ||., ||, and | are all matched as barline tokens. Synced the same change to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. Added four rows to syntax-test.chart exercising ||:, :||, :||x2, :||:, ||, and ||. All 407 tests pass.

## sync-textmate-simile-mark
Added a 'simile' repository entry to packages/textmate-grammar/grigson.tmLanguage.json with match `(?<=\\||\\s)%(?=\\s|\\||$)` and scope `keyword.operator.simile.grigson`. Included `#simile` in the main pattern list after `#dot`. Synced the same change to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. Added a `| C | % | % | F |` row to syntax-test.chart. All 407 tests pass.

## sync-textmate-slash-chords
Extended the 'chord' repository entry match regex in packages/textmate-grammar/grigson.tmLanguage.json to include an optional slash-bass group: `(?:(/)([A-G])([#b])?)?` appended before the lookahead. Added capture entries for group 4 (slash `/` → punctuation.separator.slash.grigson), group 5 (bass note letter → constant.other.chord.bass.grigson), and group 6 (bass accidental → constant.other.chord.accidental.grigson). Synced the same change to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. Added `| F/C | Am/E | C-/Bb | G/B |` to syntax-test.chart. All 407 tests pass.

## sync-textmate-comment-lines
Added a 'comment' repository entry to packages/textmate-grammar/grigson.tmLanguage.json with match `^#.*$` and scope `comment.line.number-sign.grigson`. Included `#comment` at the top of the main pattern list (before `#frontmatter`) so it matches first. Synced the same change to packages/vscode-extension/syntaxes/grigson.tmLanguage.json. Added `# This is a comment line` and `# Another comment at the end` to syntax-test.chart. All 407 tests pass.

## sync-tree-sitter-qualities-7b5-dash
Added '7b5' (before '7') and '-' (after 'm') to the quality rule choice in packages/tree-sitter-grammar/grammar.js, maintaining longest-match-first ordering. Regenerated the C parser with tree-sitter generate. Added 2 corpus tests to test/corpus/chords.txt: "Dominant seventh flat five chord (7b5)" (G7b5) and "Dash minor seventh chord (-)" (C-). No highlights.scm changes needed — the existing (quality) @keyword capture covers all quality alternatives. All 32 tree-sitter corpus tests pass; all 407 Vitest tests pass.

## sync-tree-sitter-extended-barlines
Replaced the single `barline` rule with `open_barline` and `close_barline` rules in packages/tree-sitter-grammar/grammar.js. open_barline covers :||:, :||, ||:, ||, | in longest-first order using token(choice(...)). close_barline covers :||x\d+:, :||:, :||x\d+, :||, ||:, ||., ||, | using token(choice(...)) with regex alternatives for the repeat-count variants. Updated `row` to use `open_barline` and `bar_tail` to use `close_barline`. Regenerated the C parser with tree-sitter generate. Updated all five existing corpus files (chords.txt, frontmatter.txt, sections.txt, song.txt, bars.txt) to use open_barline/close_barline node names. Added 9 new corpus tests in bars.txt covering all barline variants: double barline open (||), double final barline close (||), double dotted close (||.), start repeat open (||:), end repeat close (:||), end repeat with count (:||x2), end and start repeat (:||:), end repeat with count and start (:||x2:), and start+end repeat combined row (||: C | F :||). Updated queries/highlights.scm to scope (open_barline) and (close_barline) as @punctuation.delimiter. All 41 tree-sitter corpus tests pass; all 407 Vitest tests pass.

## sync-tree-sitter-simile-mark
Added `simile_mark` rule (`'%'`) to packages/tree-sitter-grammar/grammar.js. Updated `bar_tail` to be a `choice` between the existing `seq(optional($.time_signature), repeat1($.beat_slot), $.close_barline)` and the new `seq($.simile_mark, $.close_barline)`. Regenerated the C parser with `tree-sitter generate`. Created test/corpus/simile.txt with 3 corpus tests: single %, multiple consecutive %, and % with a non-single close barline. Updated queries/highlights.scm to scope `(simile_mark)` as `@keyword.operator`. All 44 tree-sitter corpus tests pass; all 407 Vitest tests pass.

## sync-tree-sitter-slash-chords
Added `slash_bass` rule (`seq('/', $.note_letter, optional($.accidental))`) to packages/tree-sitter-grammar/grammar.js. Updated `chord` to `seq($.note_letter, optional($.accidental), optional($.quality), optional($.slash_bass))`. Regenerated the C parser with `tree-sitter generate`. Added 3 corpus tests to test/corpus/chords.txt: slash chord with natural bass (F/C), slash chord with minor quality and flat bass (Am/E), slash chord with dash quality and flat bass (C-/Bb). Updated queries/highlights.scm to scope `slash_bass` `/` as `@punctuation.separator`, note letter as `@constant`, and accidental as `@operator`. All 47 tree-sitter corpus tests pass; all 407 Vitest tests pass.

## sync-tree-sitter-comment-lines
Added `comment` rule (`/#[^\r\n]*/`) to packages/tree-sitter-grammar/grammar.js. Updated `song` to include `$.comment` as the first alternative in the body's `repeat(choice(...))`. The regex avoids using `$` assertions (unsupported by tree-sitter's RE2 engine) — the trailing newline is left to be consumed by the existing `/\r?\n/` alternative. Regenerated the C parser with `tree-sitter generate`. Created test/corpus/comments.txt with 3 corpus tests: standalone comment line, comment before a section label, and comment between rows. Updated queries/highlights.scm to scope `(comment)` as `@comment`. All 50 tree-sitter corpus tests pass; all 407 Vitest tests pass.

## URL-encoded playground state (2026-03-17)
Added lz-string URL hash encoding to the playground. Editor content is compressed with LZString and stored in the #code/... URL fragment. Loads from hash on page open; updates hash on every edit (500ms debounce, replaceState). Falls back to default example for empty/invalid hashes.

## section-key (all four tasks: parse, normalise, render, tests)
Added `key: string | null` to the `Section` interface in `types.ts`. Extended the `SectionLabel` rule in `grammar.pegjs` to capture an optional ` key: VALUE` suffix using `FrontMatterValue`, validated with the same `isValidKey` predicate used in `FrontMatter`; invalid keys throw a parse error. Updated `SongBody` action to track `pendingKey` alongside `pendingLabel` and include `key: pendingKey` in every `sections.push()`. Rebuilt the generated parser with `pnpm build:grammar`. Updated `normaliseSection` to accept an optional `declaredKey` parameter and pass it to `detectKey`, replacing the hardcoded `null`; updated `normaliseSong` call site to pass `sec.key`. Updated `TextRenderer` to emit `[Label] key: X` when `section.key` is non-null. Updated `HtmlRenderer` to include `<span part="section-key">` inside the section-label div when `section.key` is non-null. Added `[Chorus] key: A` and `[Outro] key: A` annotations to `kodachrome.chart`. Added 8 new tests in `section.test.ts` covering parsing, validation, rendering, round-trip, and normalisation. All 417 tests pass.

## validate-meter-frontmatter
Added `parseMeterString(meter: string | null): TimeSignature | null` helper to `src/validator.ts` that parses a `'N/D'` string into a `TimeSignature` (returning null for null, 'mixed', or unparseable input). Changed the `effectiveTimeSig` initialisation in `semanticChecks` from a hardcoded `{ numerator: 4, denominator: 4 }` to `parseMeterString(song.meter) ?? { numerator: 4, denominator: 4 }`, so charts with a front-matter `meter` field (produced by `normaliseSong` hoisting) are validated against the correct time signature. Added imports for `parseSong`, `normaliseSong`, `TextRenderer` to `validator.test.ts`. Added 2 new tests: one for a front-matter-only `meter: 6/8` chart, and one round-trip that normalises a `(6/8)` chart, renders it (hoisting the inline token to front matter), and validates the rendered output — confirming no spurious warnings. All 419 tests pass.

## validate-bar-location-grammar
Modified both `BarTail` alternatives in `grammar.pegjs` to capture Peggy's `location()` and attach it as a non-enumerable `_sourceRange` property on each bar object via `Object.defineProperty` (enumerable: false, writable: false, configurable: false). The range values are converted from Peggy's 1-indexed line/column to 0-indexed LSP-style values (subtracting 1 from each). Using non-enumerable ensures the property is invisible to vitest's `toEqual` and `JSON.stringify`, so all existing round-trip tests remain unaffected. Rebuilt the generated parser with `pnpm build:grammar`. All 419 tests pass.

## validate-bar-location-range
Wired up `_sourceRange` non-enumerable property (attached to bars by grammar in the previous task) in `validator.ts`. Added `barRange(bar: Bar): DiagnosticRange` helper that reads the property via `Object.getOwnPropertyDescriptor` and falls back to `zeroRange()`. Replaced `range: zeroRange()` with `range: barRange(bar)` in the `diagnostics.push` call inside `semanticChecks`. Added 3 new tests: line-1 attribution for a bad bar on the second source line, line-0 attribution for a bad bar on the first line, and character > 0 (bar content starts after opening barline). All 422 tests pass.

## browser-bundle-validate-export
Added `export { validate } from './validator.js'` and `export type { Diagnostic, DiagnosticRange } from './validator.js'` to `packages/grigson/src/index.browser.ts`. The validator has no Node.js-specific imports so it is safe to include in the browser IIFE build. After rebuilding, `window.grigson.validate` is available to browser consumers. All 422 tests pass.

## playground-monaco-diagnostics
Extended the `update()` function in `packages/website/content/playground.njk` to call `grigson.validate(value)` after each content change. Maps each diagnostic to a Monaco marker object (converting 0-indexed LSP line/column to 1-indexed Monaco coordinates, and mapping severity 'error'/'warning' to `monaco.MarkerSeverity.Error`/`Warning`). Passes the markers array to `monaco.editor.setModelMarkers()` with owner `'grigson'`. Red squiggles now appear for parse errors and yellow squiggles for beat-balance warnings; both clear automatically when the chart is fixed. All 422 tests pass.

## custom-element-contract

Created the public extensibility contract types for third-party renderer elements:
- `src/renderers/contract.ts`: `GrigsonRendererElement` interface (extends HTMLElement, `renderChart(song: Song): Element`)
- `src/events.ts`: `GrigsonRendererUpdateEvent`, `GrigsonParseErrorEvent`, `GrigsonRenderErrorEvent` as typed Event subclasses
- Exported all new types from `src/index.browser.ts` and `src/index.ts`

## grigson-html-renderer-element
Created `packages/grigson/src/renderers/html-element.ts` with `GrigsonHtmlRenderer` extending `HTMLElement` and implementing `GrigsonRendererElement`. The `renderChart(song: Song): Element` method delegates to `HtmlRenderer`, builds the rendered HTML string, then injects styles + HTML into a wrapper div via `setHTMLUnsafe()` and returns it. Styles (CSS custom properties, dark mode, shadow parts) were moved from `GrigsonChart._getStyles()` into `GrigsonHtmlRenderer._getStyles()`. `attributeChangedCallback` dispatches `GrigsonRendererUpdateEvent` when `notation-preset` changes. Exported `GrigsonHtmlRenderer` from `src/index.browser.ts`.

## grigson-chart-renderer-discovery
Refactored GrigsonChart to discover and delegate to a child renderer element. Removed `notation-preset` from `observedAttributes` and all related config logic. Removed `_getStyles()` from `GrigsonChart`. Added `_findRenderer()` which scans light DOM children for the first element with `typeof renderChart === 'function'`, falling back to `new GrigsonHtmlRenderer()`. Updated shadow root to contain only `:host { display: block }` style. Replaced all `innerHTML` usage with `replaceChildren()`. Also updated `register.ts` to register `GrigsonHtmlRenderer` alongside `GrigsonChart` (prerequisite for the fallback to work in environments like happy-dom that require elements to be defined before `new` construction). Added `innerHTML` fallback in `GrigsonHtmlRenderer.renderChart()` for environments that don't support `setHTMLUnsafe` (e.g. happy-dom). Updated element tests to remove `notation-preset` on GrigsonChart and add renderer discovery / first-wins tests.

## grigson-chart-external-template
Added external template support to GrigsonChart via a `template` attribute containing the ID of a `<template>` element elsewhere in the document. Refactored source resolution into a `_resolveTemplate()` helper that checks for an inline `<template>` child first, then falls back to `document.getElementById()`. If neither is found the shadow root is cleared. Added 'template' to `observedAttributes` so changing the ID triggers a re-render via `attributeChangedCallback`. Added 4 new tests: external template by ID, inline-wins-over-external precedence, nonexistent ID renders nothing, and re-render on attribute change.

## update-register-and-exports
Verified that `register.ts` already registers both `grigson-html-renderer` and `grigson-chart`, and that `index.browser.ts` already exports `GrigsonHtmlRenderer`, `GrigsonRendererUpdateEvent`, `GrigsonParseErrorEvent`, `GrigsonRenderErrorEvent`, and the `GrigsonRendererElement` interface. No code changes required; marked complete.

## grigson-chart-observers
Added reactive re-rendering to GrigsonChart in packages/grigson/src/element.ts:
- _childObserver (MutationObserver { childList: true } on the element) — started in connectedCallback, triggers update() when light DOM children change
- _templateObserver (MutationObserver { childList, subtree, characterData } on template.content) — attached per-update() call after resolving the active template, replaces previous observer each time
- grigson:renderer-update event listener — calls update() when a child renderer dispatches a config change event
- disconnectedCallback — disconnects both observers and removes the event listener
All 426 tests pass.

## grigson-chart-error-events
Added typed error event dispatching to GrigsonChart.update(). Imported GrigsonParseErrorEvent and GrigsonRenderErrorEvent from events.ts. In the parse error catch block, dispatch new GrigsonParseErrorEvent(parseError) after rendering the fallback div. In the render error catch block, dispatch new GrigsonRenderErrorEvent(renderError) after rendering the fallback div. All 426 tests pass.

## grigson-text-renderer-package
Created packages/grigson-text-renderer as a reference renderer implementation outside the core grigson package. The package includes: GrigsonTextRenderer extending HTMLElement and implementing GrigsonRendererElement — renderChart() calls TextRenderer.render(song) and returns the output wrapped in a <pre> element; a side-effect-free element.ts; register.ts calling customElements.define; index.ts and index.browser.ts exports; package.json with workspace dependency on grigson; tsconfig.json and vite.config.ts mirroring the core package build setup; vitest.config.ts with passWithNoTests; and README.md. All four build outputs (ESM + IIFE for both browser and register bundles) compile cleanly. Full test suite passes (426 tests across the monorepo).

## grigson-svg-renderer-package
Created packages/grigson-svg-renderer as a stub SVG renderer package. Mirrors the grigson-text-renderer structure: GrigsonSvgRenderer extends HTMLElement and implements GrigsonRendererElement — renderChart() creates and returns an <svg> element containing a <text> element with "Under construction". Includes register.ts, index.ts, index.browser.ts, package.json (workspace dependency on grigson), tsconfig.json, vite.config.ts, and vitest.config.ts (passWithNoTests). All four build outputs compile cleanly. Full test suite passes (426 tests).

## custom-element-tests
Added missing unit tests for `GrigsonChart` and `GrigsonHtmlRenderer` in `src/element.test.ts`. Added `happy-dom` as a devDependency to the grigson package and configured `environmentMatchGlobs` in `vitest.config.ts` to apply the happy-dom environment to `element.test.ts` (the per-file `// @vitest-environment happy-dom` directive is kept for clarity). New tests cover: `GrigsonParseErrorEvent` dispatch on parse failure, `GrigsonRenderErrorEvent` dispatch and fallback div when `renderChart()` throws, re-render triggered by `template.content` mutation, re-render triggered by a `grigson:renderer-update` event bubbling from a child, and `GrigsonHtmlRenderer.renderChart()` returning an element with the expected shadow parts (`song`, `row`, `barline`, `chord`, `chord-root`, `chord-suffix`, `frontmatter`, `frontmatter-value`). Error event tests use programmatic element creation to ensure the event listener is registered before connection, avoiding double-fire from happy-dom's async MutationObserver. 432 tests pass.
