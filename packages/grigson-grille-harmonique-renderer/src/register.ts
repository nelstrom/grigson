import { GrigsonGrilleHarmoniqueRenderer } from './element.js';
if (typeof customElements !== 'undefined') {
  customElements.define('grigson-grille-harmonique-renderer', GrigsonGrilleHarmoniqueRenderer);
}
