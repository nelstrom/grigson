---
layout: base.njk
title: Notation Presets
permalink: /customize/notation/
---
<script>
  window.grigsonRegister.definePreset('ascii', {
    halfDiminished: 'm7b5',
    diminished: 'dim',
    maj7: 'maj7',
    dim7: 'dim7',
    dom7flat5: '7b5'
  });
</script>

<h1>Notation Presets</h1>
<p>
  A <em>notation preset</em> controls how quality suffixes are rendered — things like
  whether a minor-seventh chord reads <em>m7</em> or <em>MI<sup>7</sup></em>, and whether
  half-diminished reads <em>ø</em> or <em>m7b5</em>.
</p>

<h2>Built-in presets</h2>
<p>
  The following presets are available without any registration:
</p>
<ul>
  <li><strong>default</strong> — Unicode glyphs (♭ ♯ ø ° △)</li>
  <li><strong>realbook</strong> — Real Book dialect (<small>MI</small> <small>MA</small><sup>7</sup> <small>DIM</small>. etc.)</li>
</ul>

<div style="display:grid; grid-template-columns:1fr 1fr 1fr; gap:2rem; align-items:start">
  <div>
    <h3>Default preset</h3>
    <p>Unicode glyphs: ♭ ♯ ø ° △</p>
  </div>
  <div>
    <h3>&ldquo;realbook&rdquo; preset</h3>
    <p>Real Book dialect: <small>MI</small> <small>MA</small><sup>7</sup> <small>DIM</small>.</p>
  </div>
  <div>
    <h3>Custom &ldquo;ascii&rdquo; preset</h3>
    <p>ASCII quality spellings: m7b5 dim maj7</p>
  </div>
</div>
<grigson-chart normalise style="display:grid; grid-template-columns:1fr 1fr 1fr; gap:2rem">
  <grigson-html-renderer></grigson-html-renderer>
  <grigson-html-renderer notation-preset="realbook"></grigson-html-renderer>
  <grigson-html-renderer notation-preset="ascii"></grigson-html-renderer>
  <template>{{ 'notation-demo' | readChart }}</template>
</grigson-chart>

<h2>How it works</h2>
<p>
  Register a preset once using <code>window.grigsonRegister.definePreset()</code>
  (from <code>grigson-register.iife.js</code> — the same bundle the custom elements use):
</p>
{% highlight "javascript" %}window.grigsonRegister.definePreset('ascii', {
  halfDiminished: 'm7b5',
  diminished: 'dim',
  maj7: 'maj7',
});{% endhighlight %}
<p>
  Then reference it by name on <code>&lt;grigson-html-renderer&gt;</code>:
</p>
{% highlight "html" %}<grigson-chart normalise>
  <grigson-html-renderer notation-preset="ascii"></grigson-html-renderer>
  <template>...</template>
</grigson-chart>{% endhighlight %}
<p>
  The <code>notation-preset</code> attribute belongs to <code>&lt;grigson-html-renderer&gt;</code>,
  not <code>&lt;grigson-chart&gt;</code>. Multiple renderer children inside one
  <code>&lt;grigson-chart&gt;</code> each render the same parsed song independently:
</p>
{% highlight "html" %}<grigson-chart normalise>
  <grigson-html-renderer></grigson-html-renderer>
  <grigson-html-renderer notation-preset="ascii"></grigson-html-renderer>
  <template>...</template>
</grigson-chart>{% endhighlight %}

<h2>Preset fields</h2>
<p>
  Unspecified fields fall back to <code>DEFAULT_PRESET</code>. For the complete list of fields
  with their default values and descriptions, see the
  <a href="{{ '/dev/api/notation/' | url }}">Notation Presets API reference</a>
  (<code>NotationPreset</code>, <code>DEFAULT_PRESET</code>, <code>REALBOOK_PRESET</code>,
  <code>definePreset</code>, <code>resolvePreset</code>).
</p>
